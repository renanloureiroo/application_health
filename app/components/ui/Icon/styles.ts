import { ViewStyle } from 'react-native'

export const $container: ViewStyle = {
  alignItems: 'center',
  justifyContent: 'center',

  opacity: 1,
}

export const $container_pressed: ViewStyle = {
  opacity: 0.5,
}
